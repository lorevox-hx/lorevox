Lorevox — 7.3 Handoff

Version: 7.3
Date: 2026-03-20
Purpose: Operational handoff for local-first, offline-capable Lorevox system

1. Scope

This document defines:

how to start the system

how to run it fully offline

file locations and required assets

validation procedure

current completion status

remaining work required for production readiness

This document is execution-focused and assumes no prior context.

2. System State
2.1 Architecture

Archive → History → Memoir separation enforced

Timeline Spine (6 life periods) is the primary structure

Multi-pass interview system:

Pass 1 — timeline seed

Pass 2A — chronological walk

Pass 2B — narrative depth

2.2 Runtime

runtime71 active end-to-end

Propagated per turn:

pass / era / mode

affectState / affectConfidence

cognitiveMode

fatigueScore

Prompt system:

prompt_composer.py injects runtime every turn

WebSocket:

chat_ws.py forwards runtime on each turn

2.3 UI

Entry:

ui/lori7.3.html

Lori is persistent (companion model)

Timeline-driven interface

Debug overlay available (Ctrl+Shift+D)

3. Offline Conversion (Completed)
3.1 Tailwind

CLI installed:

npm install -D @tailwindcss/cli

Input:

ui/css/tailwind-input.css

Build:

npx @tailwindcss/cli \
  -i ./ui/css/tailwind-input.css \
  -o ./ui/css/tailwind.min.css \
  --minify

Output:

ui/css/tailwind.min.css

CDN removed from HTML

3.2 MediaPipe

Fully vendored:

ui/vendor/mediapipe/

emotion.js patched for local loading

3.3 Result

All UI dependencies are local:

Component	Status
Tailwind	Local
CSS	Local
MediaPipe JS	Local
MediaPipe WASM	Local
Emotion system	Local
4. Directory Layout (Authoritative)
C:\Users\chris\lorevox\

ui\
  lori7.3.html

  css\
    tailwind.min.css
    tailwind-input.css
    *.css

  js\
    app.js
    state.js
    interview.js
    emotion.js
    emotion-ui.js
    cognitive-auto.js
    debug-overlay.js

  vendor\
    mediapipe\
      face_mesh\
      camera_utils\

server\
  code\
    api\
      chat_ws.py
      prompt_composer.py
      session_engine.py
      session_vitals.py
      skills.py
      hooks.py
      reflection.py
5. Startup Procedure
Terminal 1 — Backend
cd /mnt/c/Users/chris/lorevox
source .venv-gpu/bin/activate

export MODEL_PATH=/mnt/c/Llama-3.1-8B/hf/Meta-Llama-3.1-8B-Instruct
export HF_HOME=/mnt/c/Llama-3.1-8B/hf_home
export HF_HUB_OFFLINE=1
export TRANSFORMERS_OFFLINE=1

export DATA_DIR=/mnt/c/lorevox_data
export ATTN_IMPL=sdpa
export LV_DEV_MODE=1

mkdir -p /mnt/c/lorevox_data/{db,interview,logs,cache_audio,memory,projects,tts_cache,uploads,media}

cd server
export PYTHONPATH=/mnt/c/Users/chris/lorevox/server

Verify model:

python - <<'PY'
from code.api.api import _load_model
m, t = _load_model()
print("MODEL OK:", type(m).__name__, type(t).__name__)
PY

Start:

bash launchers/run_gpu_8000.sh
Terminal 2 — TTS
cd /mnt/c/Users/chris/lorevox
bash launchers/run_tts_8001.sh
UI
file:///C:/Users/chris/lorevox/ui/lori7.3.html
6. Offline Validation Procedure

Start backend + TTS

Open UI

Disable network

Verify:

UI renders correctly

chat responses work

runtime logs visible

camera initializes

no external network requests

7. Completion Checklist
✅ Completed
Architecture

 Archive → History → Memoir model

 Timeline Spine structure (6 periods)

 Multi-pass interview design

Backend Runtime

 runtime71 propagation

 prompt_composer runtime injection

 WebSocket runtime forwarding

 logging (compact + dev mode)

UI State & Behavior

 state.js runtime model

 app.js runtime propagation

 interview.js pass/era handling

 timeline rendering (spine-aware)

Cognitive & Debug Layer

 cognitive-auto mode switching

 debug overlay (live runtime view)

Offline Conversion

 Tailwind compiled locally

 CDN removed

 MediaPipe vendored

 emotion.js patched

 CSS fully local

Validation Harness

 prompt pipeline tests

 model test harness (no-model + real mode)

⚠ In Progress / Not Fully Integrated

 Affect engine → live runtime wiring

 SessionVitals → automatic fatigue scoring

 SessionEngine → backend-driven pass control

 Agent loop → full lifecycle integration

 Memory/RAG → real retrieval module

⛔ Not Started / Deferred

 Scene-based memory model (replace flat memories)

 Memoir generation aligned to timeline spine

 Backend-controlled session advancement

 Timeline-aware agent routing

 Legacy UI fallback removal

8. Known Constraints
Browser

camera + wasm may fail under file://

Paths

UI depends on strict relative structure

Backend Authority

UI still controls some session flow (temporary)

9. Required Next Actions

Run full offline validation

Confirm zero external requests

Wire affect → runtime state

Enable SessionVitals in live loop

Move pass advancement to backend

Complete agent loop retrieval integration

10. Operational Summary

System is local-first and offline-capable

UI dependencies are fully internalized

Runtime pipeline is stable and observable

Backend logic is partially authoritative

Final readiness depends on remaining integrations