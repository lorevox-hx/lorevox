In main.py update it:  What’s missing (relative to your WS goal)
* Nothing in main.py itself is blocking WS. The “gap” is that your UI is still using SSE-style POST streaming to /api/chat/stream and parsing data: lines, but your target design is WebSocket streaming (stateful connection, start_turn/token/done protocol).
* Recommendation: in the backend, keep one clear contract:
o /api/chat/stream = WebSocket (best for “true live”)
The “real” work now is: update the UI to WS + tighten endpoints.  UI (lorevox-v4.html) — comments + recommendations
Biggest mismatch right now
Your UI currently does this:
* API.CHAT = ORIGIN + "/api/chat/stream" ?
* but then calls it with:
o fetch(API.CHAT, { method:"POST", ... })
o reads res.body.getReader()
o parses newline-delimited chunks with data: prefixes
That is SSE-style logic, not WebSocket logic. 
lorevox-v4
Second mismatch: STT
Right now your UI still uses browser SpeechRecognition (webkit SR) in toggleRecording() rather than MediaRecorder ? Whisper upload. 
lorevox-v4

That directly conflicts with your “local Whisper + WS chat” direction.
TTS separation
Your UI TTS flow is calling http://localhost:8001/api/tts/speak_stream and decoding a wav_b64 JSON message. 
lorevox-v4

That’s fine to keep as-is for now (you explicitly said don’t mess with the Coqui side).
“App workstation” upgrades
Your “inspector tabs / timeline / diffs / profile panel” ideas are good, but they need one important constraint to stay fast:
* Do not add extra network calls on every token.
* Only refresh inspector panels after done (or on explicit button click).
Plan of action (next steps, no implementation yet)
Phase 1 — Make transport correct (WS end-to-end)
1. Replace streamChat() (POST/SSE) with a single WS client:
o connect once (auto-reconnect)
o start_turn send
o receive: status, token, tool, done, error
o support cancel_turn
2. Update UI bubbles to support:
o one “streaming assistant bubble” that grows with tokens
o typing indicator on status: thinking/working
3. Keep session concept consistent:
o your UI uses state.conv_id as the session id already — use that as session_id in WS messages.
Deliverable outcome: chat feels instant, token-by-token, and you stop fighting the SSE parser entirely.

Phase 2 — Make STT correct (MediaRecorder ? Whisper)
1. Replace the current toggleRecording() SpeechRecognition version with:
o MediaRecorder capture ? Blob ? POST /api/stt/transcribe (or whatever your STT router exposes)
o append transcript into #chatInput
2. Keep a small UX:
o status: Listening ? Transcribing ? Ready
Deliverable outcome: you’re truly local-first for speech (no browser cloud STT).

Phase 3 — Make UI feel “alive” (without adding latency)
Implement the “workstation” features in an order that won’t destabilize anything:
1. Typing indicator (pure UI)
2. Timeline entries + jump-to-turn (pure UI, no backend dependency)
3. Tools panel:
o only depends on backend sending {type:"tool", ...} WS messages
4. Profile / Memory / Diffs panels
o only refresh after done
o these require REST endpoints (you may already have some under /api/profiles/...; we’ll align the UI to your real paths instead of inventing new ones)
Deliverable outcome: the UI becomes a real “studio console” but remains fast.

Phase 4 — Post-turn persistence contract (backend polish)
You already stated the contract perfectly:
* hot loop: no DB writes
* after done: persist_turn_transaction(...) does one atomic write and optional modules
In this phase, we:
1. Ensure chat router accumulates full text in RAM
2. After generation ends, run one transaction:
o write chat_turn
o update memory summary
o tool logs (if any)
o optional: snapshot profile
Deliverable outcome: maximum smoothness + durability.

